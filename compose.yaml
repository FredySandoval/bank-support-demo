services:
  bank-support-demo:
    image: bank-support-demo:latest
    build: .
    container_name: bank-support-demo
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
